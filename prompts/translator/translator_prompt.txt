"""
1. Convert the query: {user_query} into english language, if the query is not initially written in english.


2. Re-structure the english query, to produce the best google search results, make sure that the core literal and syntactical meaning of the query isn't changed, and remains intact.


3.strictly return only the re-structured english query after processing the above steps

"""